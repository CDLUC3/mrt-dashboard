<% f = file_info_maybe %>

<% if md = f.to_s.match(/\/producer%2F([^\/]+)/) then %>
<li><b><%= link_to(md[1], "/show/view/#{clean_id(f.to_uri)}") %></b>, 
  (<%= f.first(Mrt::File['mediaType']) %>, <%= number_to_human_size(f.first(Mrt::Base.size).value) %>)
<% end %>
