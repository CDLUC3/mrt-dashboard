<% version = line_sidebar %>
<%= link_to "Version #{version.identifier}",
            :controller     => :version,
            :action         => :index,
            :group          => params[:group],
            :object         => params[:object],
            :version        => version[RDF::DC.identifier][0].to_s %>:
<%= merritt_time(version.created) %><br/>
