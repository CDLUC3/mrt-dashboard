<% content_for :title do %>
  UC3 Merritt: Submission Received
<% end %>

<main id="maincontent">
  <section>
    <h1>Submission Received</h1>

    <div class="breadcrumbs">
      <ol>
      <li><%= link_to('Merritt', :controller => 'home', :action => 'index') %></li>
      <li><%= link_to(
              "Collection: #{current_group.description}",
              controller: 'collection',
              action: 'index',
              group: current_group.id
          ) %></li>
      <li>Submission Received</li>
      </ol>
    </div>

    <p>
      You have requested to add data to the repository. Your request is now
      in process.
    </p>

    <p>
      The id for your request is <%= @batch_id %>.
    </p>
  </section>

  <aside>
    <%= render(partial: '/shared/side_lookup') %>
  </aside>
</main>
