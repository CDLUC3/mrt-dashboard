Merritt UI
==========

Getting started
---------------

Copy `config/database.yml.example` to `config/database.yml` and
configure password. Copy `config/ldap.yml.example` to
`config/ldap.yml` and configure password.

Run:

    bundle install

to install required gems.

To run the test suite, run:

    rake test

