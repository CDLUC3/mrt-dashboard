# This is a simple file to test SSM resolution
test: &test
  user: "{!ENV: USER}"
  rb: "{!ENV: SSM_ROOT_PATH}"
  host: "{!SSM: inv/db-host !DEFAULT: ZZZ}"
  foo: bar

stage:
  <<: *test

development:
  <<: *test
